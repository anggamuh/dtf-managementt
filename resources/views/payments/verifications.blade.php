@extends('layouts.app')
@section('title','Verifikasi Pembayaran')
@section('content')
<div><p class="text-sm font-medium text-blue-600 dark:text-blue-400">Keuangan</p><h1 class="mt-1 text-2xl font-bold">Verifikasi Pembayaran</h1><p class="mt-2 text-sm text-slate-500">Tinjau bukti transfer sebelum pesanan diproses.</p></div>
<div class="mt-6 grid gap-5">@forelse($orders as $order)@php $c=$order->paymentConfirmations->first(); @endphp<div class="app-card p-5 sm:p-6" x-data="{rejectOpen:false}"><div class="flex flex-col justify-between gap-3 sm:flex-row sm:items-start"><div><div class="text-lg font-bold">{{ $order->order_number }}</div><p class="mt-1 text-sm text-slate-500">{{ $order->user?->name ?? 'Customer' }} · {{ $order->branch?->name ?? '—' }}</p></div><span class="app-badge bg-amber-100 text-amber-700 dark:bg-amber-900/40 dark:text-amber-300">Menunggu Verifikasi</span></div>@if($c)<div class="mt-5 grid gap-4 rounded-xl bg-slate-50 p-4 text-sm dark:bg-slate-800/60 sm:grid-cols-3"><div><span class="block text-xs text-slate-500">Pengirim</span><b>{{ $c->sender_name }}</b></div><div><span class="block text-xs text-slate-500">Bank</span><b>{{ $c->sender_bank }}</b></div><div><span class="block text-xs text-slate-500">Nominal</span><b>Rp {{ number_format($c->amount,0,',','.') }}</b></div></div><a class="app-btn app-btn-secondary mt-4 min-h-11 w-full sm:w-auto" href="{{ route('payment-confirmations.proof',$c) }}">Lihat bukti pembayaran</a><div class="mt-5 flex flex-col gap-3 border-t border-slate-200 pt-5 dark:border-slate-700 sm:flex-row"><form method="POST" action="{{ route('payment-confirmations.approve',$c) }}" class="sm:flex-1">@csrf @method('PATCH')<button class="app-btn app-btn-primary min-h-11 w-full">Setujui Pembayaran</button></form><button type="button" class="app-btn app-btn-danger min-h-11 sm:flex-1" @click="rejectOpen=true">Tolak Pembayaran</button></div><div x-cloak x-show="rejectOpen" x-transition class="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/60 p-4" @keydown.escape.window="rejectOpen=false"><div class="w-full max-w-md rounded-2xl bg-white p-5 shadow-2xl dark:bg-slate-900" @click.outside="rejectOpen=false"><div class="flex items-center justify-between"><h2 class="font-semibold">Tolak pembayaran</h2><button type="button" class="app-icon-btn" @click="rejectOpen=false" aria-label="Tutup modal"><svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg></button></div><form method="POST" action="{{ route('payment-confirmations.reject',$c) }}" class="mt-4">@csrf @method('PATCH')<label class="text-sm font-medium">Alasan penolakan<textarea name="rejection_reason" class="app-input mt-2 text-base" rows="4" required></textarea></label><div class="mt-4 flex flex-col-reverse gap-2 sm:flex-row sm:justify-end"><button type="button" class="app-btn app-btn-secondary min-h-11" @click="rejectOpen=false">Batal</button><button class="app-btn app-btn-danger min-h-11">Tolak Pembayaran</button></div></form></div></div>@endif</div>@empty<div class="app-card app-empty"><div class="app-empty-title">Tidak ada pembayaran menunggu verifikasi</div><div class="app-empty-desc">Semua bukti pembayaran sudah diproses.</div></div>@endforelse</div><div class="mt-5">{{ $orders->links() }}</div>
@endsection
