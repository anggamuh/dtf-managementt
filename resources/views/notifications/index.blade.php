@extends('layouts.app')
@section('title','Notifikasi')
@section('content')
<div><p class="text-sm font-medium text-blue-600 dark:text-blue-400">Pusat informasi</p><h1 class="mt-1 text-2xl font-bold">Notifikasi</h1><p class="mt-2 text-sm text-slate-500">Pembaruan operasional dan pesanan cabang.</p></div><div class="app-card mt-6 overflow-hidden"><div class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($notifications as $item)<a href="{{ route('notifications.visit',$item->id) }}" class="block min-h-20 p-4 transition hover:bg-slate-50 dark:hover:bg-slate-800/60 {{ $item->read_at?'':'bg-blue-50/70 dark:bg-blue-950/30' }}"><div class="flex gap-3"><span class="mt-2 h-2.5 w-2.5 shrink-0 rounded-full {{ $item->read_at?'bg-slate-300':'bg-blue-600' }}"></span><div class="min-w-0"><b class="block">{{ $item->data['title']??'Notifikasi' }}</b><p class="mt-1 text-sm text-slate-500">{{ $item->data['message']??'' }}</p><small class="mt-2 block text-xs text-slate-400">{{ $item->created_at->diffForHumans() }}</small></div></div></a>@empty<div class="app-empty"><div class="app-empty-title">Belum ada notifikasi</div><div class="app-empty-desc">Notifikasi baru akan muncul di sini.</div></div>@endforelse</div></div><div class="mt-5">{{ $notifications->links() }}</div>
@endsection
